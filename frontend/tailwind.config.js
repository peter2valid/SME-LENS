/** @type {import('tailwindcss').Config} */
export default {
    content: [
        "./index.html",
        "./src/**/*.{js,ts,jsx,tsx}",
    ],
    // Tailwind v4: config is now primarily done in CSS @theme blocks
    // This file is kept for compatibility and content paths
}
